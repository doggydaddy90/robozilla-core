# Skill Contracts (Bundled)
#
# This directory is intentionally empty in build mode until SkillContracts are authored.
# The runtime tolerates an empty directory and will load contracts when they exist.

